# ═══════════════════════════════════════════════════════════════════
# Rubinho Scripts - Environment Variables Configuration
# ═══════════════════════════════════════════════════════════════════
#
# 📋 INSTRUCTIONS:
#   1. Copy this file to .env:  cp .env.example .env
#   2. Fill in your actual values below
#   3. Never commit .env to version control (it's gitignored)
#
# 🔒 SECURITY:
#   - Set file permissions: chmod 600 .env
#   - Keep your API keys private and secure
#   - Rotate keys immediately if accidentally exposed
#   - Never share your .env file
#
# ═══════════════════════════════════════════════════════════════════

# ────────────────────────────────────────────────────────────────────
# 🔑 GIT CONFIGURATION (REQUIRED for Git setup scripts)
# ────────────────────────────────────────────────────────────────────
# These values are used by the Git configuration scripts (01-configure-git.sh)
# If not set here, the scripts will prompt you during installation.

# Your full name (e.g., "John Doe")
# Used for: git config user.name
GIT_USER_NAME=

# Your email address (e.g., "john.doe@example.com")
# Used for: git config user.email
# Also used for: SSH key generation (11-configure-ssh.sh)
GIT_USER_EMAIL=

# ────────────────────────────────────────────────────────────────────
# 🤖 ANTHROPIC / CLAUDE API (REQUIRED for Claude Code CLI)
# ────────────────────────────────────────────────────────────────────
# Status: ⚠️  REQUIRED if using Claude Code CLI or Task Master AI
#
# 📍 Where to get: https://console.anthropic.com/settings/keys
#   1. Sign in or create an account
#   2. Go to Settings → API Keys
#   3. Click "Create Key"
#   4. Copy the key (starts with 'sk-ant-')
#
# 📝 Format: sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# 💰 Cost: Pay-as-you-go pricing (see Anthropic website)
#
ANTHROPIC_API_KEY=

# ────────────────────────────────────────────────────────────────────
# 📋 TASK MASTER AI (OPTIONAL - uses Claude API key by default)
# ────────────────────────────────────────────────────────────────────
# Status: ✅ OPTIONAL
#
# Task Master AI can use the same ANTHROPIC_API_KEY configured above.
# No separate API key is needed unless you want to use a different one.
#
# 📍 Where to get: Same as ANTHROPIC_API_KEY
# 📖 Documentation: https://docs.task-master.dev/
# 🌐 Website: https://www.task-master.dev/
#
# Note: Task Master is configured via MCP in ~/.cursor/mcp.json
#       This .env file is for CLI usage only.

# ────────────────────────────────────────────────────────────────────
# 🔍 PERPLEXITY API (OPTIONAL - for research-backed operations)
# ────────────────────────────────────────────────────────────────────
# Status: ✅ OPTIONAL
#
# Used by Task Master AI for research-backed task generation and updates.
# Only needed if you want to use Perplexity's research capabilities.
#
# 📍 Where to get: https://www.perplexity.ai/settings/api
#   1. Sign in or create an account
#   2. Go to Settings → API
#   3. Create a new API key
#   4. Copy the key (starts with 'pplx-')
#
# 📝 Format: pplx-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# 💰 Cost: Pay-as-you-go pricing (see Perplexity website)
#
PERPLEXITY_API_KEY=

# ────────────────────────────────────────────────────────────────────
# 🚀 OPENAI API (OPTIONAL - alternative AI provider)
# ────────────────────────────────────────────────────────────────────
# Status: ✅ OPTIONAL
#
# Alternative AI provider for Task Master or other tools.
# Only needed if you want to use OpenAI models instead of Claude.
#
# 📍 Where to get: https://platform.openai.com/api-keys
#   1. Sign in or create an account
#   2. Go to API Keys section
#   3. Create a new secret key
#   4. Copy the key (starts with 'sk-')
#
# 📝 Format: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# 💰 Cost: Pay-as-you-go pricing (see OpenAI website)
#
OPENAI_API_KEY=

# ────────────────────────────────────────────────────────────────────
# 🌐 GOOGLE API (OPTIONAL - for Google AI models)
# ────────────────────────────────────────────────────────────────────
# Status: ✅ OPTIONAL
#
# Used for Google AI models (Gemini, etc.) in Task Master or other tools.
#
# 📍 Where to get: https://aistudio.google.com/app/apikey
#   1. Sign in with your Google account
#   2. Create a new API key
#   3. Copy the key
#
# 📝 Format: Varies (see Google AI documentation)
# 💰 Cost: Check Google AI pricing
#
GOOGLE_API_KEY=

# ────────────────────────────────────────────────────────────────────
# 💻 CURSOR IDE (NO CONFIGURATION NEEDED HERE)
# ────────────────────────────────────────────────────────────────────
# Status: ℹ️  INFO
#
# Cursor IDE configuration is handled through the Cursor app itself.
# No API key is needed in this file.
#
# 📍 Setup:
#   1. Install Cursor from: https://cursor.sh/
#   2. Open Cursor Settings (Cmd+, or Ctrl+,)
#   3. Configure AI provider (Claude, OpenAI, etc.)
#   4. Enable Task Master MCP in Settings → MCP
#
# 📖 Task Master MCP config: ~/.cursor/mcp.json
#    Add your API keys there for MCP integration.

# ────────────────────────────────────────────────────────────────────
# 📝 NOTES
# ────────────────────────────────────────────────────────────────────
#
# ✅ REQUIRED Variables:
#    - GIT_USER_NAME (or will be prompted)
#    - GIT_USER_EMAIL (or will be prompted)
#    - ANTHROPIC_API_KEY (if using Claude/Task Master)
#
# ✅ OPTIONAL Variables:
#    - PERPLEXITY_API_KEY (for research features)
#    - OPENAI_API_KEY (alternative AI provider)
#    - GOOGLE_API_KEY (for Google AI models)
#
# 🔒 Security Best Practices:
#   1. Never commit .env to version control
#   2. Use chmod 600 .env to restrict file permissions
#   3. Rotate keys regularly
#   4. Use different keys for different environments
#   5. Never share your .env file or API keys
#
# 📚 Additional Resources:
#   - Task Master Docs: https://docs.task-master.dev/
#   - Anthropic Console: https://console.anthropic.com/
#   - Cursor Website: https://cursor.sh/
#   - Perplexity API: https://www.perplexity.ai/settings/api
#
# ═══════════════════════════════════════════════════════════════════
